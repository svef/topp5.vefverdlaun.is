
mixin item(title, href)
  p
    if href
      a(href=href target='_blank'): b=title
    else
      b=title

mixin category(title, subtitle)
  .column.small-12.large-6.xlarge-4.text-center
    h3
      =title
      if subtitle
        br
        small=subtitle
    block
